using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class excess : MonoBehaviour 
{
//from AnimalController -> Update
		/*else if(myMove == LEFT)
		{
			if(!isTurning)
			{
				isTurning = true;
				curAngle = transform.eulerAngles;
				newAngle = curAngle.y - moveSpeed;	//moveSpeed = movement angle, so it always takes 1 second to turn, regardless of how far
			}
			else if(isTurning)
			{
				if(curAngle.y > newAngle)
				{
					curAngle.y = Mathf.MoveTowards(curAngle.y, newAngle, Time.deltaTime * turnSpeed);
					transform.eulerAngles = curAngle;
				}
				else 
					isTurning = false;
			}				
		}
		else if(myMove == RIGHT)
		{
			if(!isTurning)
			{
				isTurning = true;
				curAngle = transform.eulerAngles;
				newAngle = curAngle.y + moveSpeed;
			}
			else if(isTurning)
			{
				if(curAngle.y < newAngle)
				{
					curAngle.y = Mathf.MoveTowards(curAngle.y, newAngle, Time.deltaTime * turnSpeed);
					transform.eulerAngles = curAngle;
				}
				else 
					isTurning = false;
			}				
		}*/



			/*if(myMove == LEFT)
				newAngle = curAngle.y - 90;
			else if(myMove == RIGHT)
				newAngle = curAngle.y + 90;
			else if(myMove == CUSTOM)*/
}
